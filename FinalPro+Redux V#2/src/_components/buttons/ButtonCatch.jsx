import React from 'react';

const ButtonCatch = (props) => {
    return (
        <button href="#" className="cart-btn btn btn-danger btn-block btn-lg" onClick={props.onClick}>Catch me!</button>
    )
}

export default ButtonCatch;


