import React from 'react';

const ButtonDelete = (props) => {
    return (
        <button href="#" className="btn btn-secondary btn-block btn-lg" onClick={props.onClick}>Delete</button>
    )
}

export default ButtonDelete;